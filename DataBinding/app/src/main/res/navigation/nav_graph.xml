<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.databinding.view.activities.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <activity
        android:id="@+id/dataBindingActivity"
        android:name="com.example.databinding.view.activities.DataBindingActivity"
        android:label="DataBindingActivity" />
    <activity
        android:id="@+id/lifeCycleAwareActivity"
        android:name="com.example.databinding.view.activities.LifeCycleAwareActivity"
        android:label="activity_life_cycle_aware"
        tools:layout="@layout/activity_life_cycle_aware" />
</navigation>